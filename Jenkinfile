pipeline {
agent none
    stages {
        stage("SomeStageName") {
        steps {
           script {
              println "\n\n-- Running on machine: " + "hostname".execute().text
                   }
              }
           }
        }
    }
