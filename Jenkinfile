pipeline {
   agent any

   stages {
      stage('Kaam') {
        steps {
          echo 'Building...'
          echo "Running ${env.BUILD_ID} ${env.BUILD_DISPLAY_NAME} on ${env.NODE_NAME} and JOB ${env.JOB_NAME}"
        }
   }
   stage('Hoaya') {
     steps {
        echo 'Testing...'
     }
   }
   stage('hai') {
     steps {
       echo 'Deploying...'
     }
   }
  }
}
